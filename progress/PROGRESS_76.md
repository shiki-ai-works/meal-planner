# PROGRESS_76

> **デモ料理種類サマリー復帰** — 2026-05-25

前回、`/demo` 上部に買い物残数と平均 kcal を足した。今回は、その代わりに見えなくなっていた料理種類数を戻し、さらに外食枠を料理種類から外すように整えた。

## やったこと

### Step 1: 料理種類数をサマリーへ戻した

`/demo` 上部のサマリーカードに `料理` を復帰させた。

固定・外食・料理・買い物残・平均 kcal が並ぶので、献立の状態をタブ移動前に読める。

### Step 2: 外食枠を料理種類から除外

`countMeals()` で外食枠を見つけたら、料理種類の集計へ進まないようにした。

買い物リストや栄養集計と同じく、「家で作るもの」を基準にした数字になる。

### Step 3: ROADMAP を更新

完了済み項目、Now、関連ドキュメントを `PROGRESS_76` へ進めた。

## 確認したこと

- `npm run check` が通る
- `/demo` に `固定` / `外食` / `料理` / `買い物残` / `平均kcal` が表示される
- `鶏の唐揚げ定食` 2 枠を外食にすると、外食が 1 -> 3、料理が 9 -> 8 に変わる
- 同じ操作で買い物残と平均 kcal も変わる

## 検証コマンド

```powershell
npm.cmd run check
git diff --check
```

## 結果

- `npm.cmd run check` -> pass
- `git diff --check` -> pass（CRLF 警告のみ）
- `/demo` ブラウザ確認 -> pass（サマリー表示、外食切替後の料理種類・買い物残・平均 kcal 更新を確認）

## 変更ファイル

- `src/app/demo/DemoClient.tsx`
- `ROADMAP.md`
- `progress/PROGRESS_76.md`
